image: gitpod/workspace-full-vnc

tasks:
  - name: Install & Start ELI
    init: npm install
    command: npm run dev

ports:
  - port: 5173 
    onOpen: ignore
  - port: 6080 
    onOpen: open-preview

github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    addComment: true
    addBadge: true
    addLabel: true
